package com.lumar.moneymanager.service;


public class TransactionValidatorTest {
/*
	private TransactionValidator validator = new TransactionValidator();
	private static List<String> HEADINGS = new ArrayList<String>();
	
	static {
		HEADINGS.add("Date");
		HEADINGS.add("Type");
		HEADINGS.add("Desc");
		HEADINGS.add("Credit");
		HEADINGS.add("Debit");
		HEADINGS.add("Balance");
	}
	
	@Test
	public void shouldFindAnInvalidTransaction() {
		String transactionUpload 
		= "22-Dec-1983	POS	Ted Baker	-	12.00	£120.00\n"
		 +"1-Dec-1983	POS	Ted Baker	-	12.00";
		
		List<String> transactions = new ArrayList<String>(Arrays.asList(transactionUpload.split("\n")));
		TransactionValidation wrapper = new TransactionValidation(transactions);
		validator.validateTransactionColumns(wrapper, HEADINGS, transactions);
		
		Assert.assertTrue(wrapper.getInvalidTransactions().size() == 1);
		Assert.assertTrue(wrapper.getInvalidTransactions().get(TransactionValidator.INVALID_NUM_OF_COLUMNS).get(0)
						  .equals("1-Dec-1983	POS	Ted Baker	-	12.00"));
		
	}
*/	
}
